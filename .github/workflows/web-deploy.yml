name: Deploy Foundation Web

on:
  push:
    branches:
      - main
    paths:
      - 'apps/web/**'
      - 'packages/ui/**'
      - 'infra/foundation-web/**'
      - '.github/workflows/web-*.yml'
      - 'package.json'
      - 'package-lock.json'
      - 'turbo.json'
      - 'tsconfig.base.json'
  workflow_dispatch:
    inputs:
      environment:
        description: Deployment environment
        required: true
        default: staging
        type: choice
        options:
          - staging
          - prod

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1
  DEPLOY_ENVIRONMENT: ${{ github.event.inputs.environment || (github.ref == 'refs/heads/main' && 'prod' || 'staging') }}
  FOUNDATION_WEB_STACK_NAME: foundation-web-${{ github.event.inputs.environment || (github.ref == 'refs/heads/main' && 'prod' || 'staging') }}
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  checks:
    name: Foundation Web Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install cfn-lint
        run: pip install cfn-lint

      - name: Validate root template
        run: cfn-lint infra/foundation-web/template.yaml

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run type check
        run: npm --workspace @olivias/web run typecheck

      - name: Build foundation web app
        run: npm --workspace @olivias/web run build

  deploy:
    name: Deploy Foundation Web
    runs-on: ubuntu-latest
    needs: [checks]
    outputs:
      frontend-url: ${{ steps.stack-outputs.outputs.frontend-url }}
      frontend-bucket: ${{ steps.stack-outputs.outputs.frontend-bucket }}
      cloudfront-distribution-id: ${{ steps.stack-outputs.outputs.cloudfront-distribution-id }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4.3.1
        with:
          role-to-assume: ${{ env.DEPLOY_ENVIRONMENT == 'prod' && secrets.AWS_PROD_ROLE_ARN || secrets.AWS_STAGING_ROLE_ARN }}
          role-session-name: GitHubActions-FoundationWeb-Deploy
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy shared foundation web stack
        env:
          DOMAIN_NAME: ${{ env.DEPLOY_ENVIRONMENT == 'prod' && vars.FOUNDATION_WEB_DOMAIN_NAME_PROD || vars.FOUNDATION_WEB_DOMAIN_NAME_STAGING || '' }}
          DOMAIN_HOSTED_ZONE_ID: ${{ env.DEPLOY_ENVIRONMENT == 'prod' && vars.FOUNDATION_WEB_DOMAIN_HOSTED_ZONE_ID_PROD || vars.FOUNDATION_WEB_DOMAIN_HOSTED_ZONE_ID_STAGING || '' }}
        run: |
          aws cloudformation deploy \
            --template-file infra/foundation-web/template.yaml \
            --stack-name "${{ env.FOUNDATION_WEB_STACK_NAME }}" \
            --region "${{ env.AWS_REGION }}" \
            --capabilities CAPABILITY_IAM \
            --no-fail-on-empty-changeset \
            --parameter-overrides \
              EnvironmentName="${{ env.DEPLOY_ENVIRONMENT }}" \
              DomainName="$DOMAIN_NAME" \
              DomainHostedZoneId="$DOMAIN_HOSTED_ZONE_ID"

      - name: Read stack outputs
        id: stack-outputs
        run: |
          FRONTEND_URL=$(aws cloudformation describe-stacks \
            --stack-name "${{ env.FOUNDATION_WEB_STACK_NAME }}" \
            --region "${{ env.AWS_REGION }}" \
            --query "Stacks[0].Outputs[?OutputKey=='WebFrontendUrl'].OutputValue" \
            --output text)

          FRONTEND_BUCKET=$(aws cloudformation describe-stacks \
            --stack-name "${{ env.FOUNDATION_WEB_STACK_NAME }}" \
            --region "${{ env.AWS_REGION }}" \
            --query "Stacks[0].Outputs[?OutputKey=='WebFrontendBucketName'].OutputValue" \
            --output text)

          CLOUDFRONT_DISTRIBUTION_ID=$(aws cloudformation describe-stacks \
            --stack-name "${{ env.FOUNDATION_WEB_STACK_NAME }}" \
            --region "${{ env.AWS_REGION }}" \
            --query "Stacks[0].Outputs[?OutputKey=='WebFrontendDistributionId'].OutputValue" \
            --output text)

          echo "frontend-url=$FRONTEND_URL" >> $GITHUB_OUTPUT
          echo "frontend-bucket=$FRONTEND_BUCKET" >> $GITHUB_OUTPUT
          echo "cloudfront-distribution-id=$CLOUDFRONT_DISTRIBUTION_ID" >> $GITHUB_OUTPUT

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build foundation web app for deployment
        run: npm --workspace @olivias/web run build

      - name: Deploy frontend assets to S3
        run: |
          aws s3 sync apps/web/dist/ "s3://${{ steps.stack-outputs.outputs.frontend-bucket }}/" \
            --region "${{ env.AWS_REGION }}" \
            --delete \
            --cache-control "public, max-age=31536000, immutable" \
            --exclude "index.html" \
            --exclude "manifest.json"

          aws s3 cp apps/web/dist/index.html "s3://${{ steps.stack-outputs.outputs.frontend-bucket }}/index.html" \
            --region "${{ env.AWS_REGION }}" \
            --cache-control "no-cache, no-store, must-revalidate" \
            --content-type "text/html"

          if [ -f "apps/web/dist/manifest.json" ]; then
            aws s3 cp apps/web/dist/manifest.json "s3://${{ steps.stack-outputs.outputs.frontend-bucket }}/manifest.json" \
              --region "${{ env.AWS_REGION }}" \
              --cache-control "no-cache, no-store, must-revalidate" \
              --content-type "application/json"
          fi

      - name: Invalidate CloudFront cache
        run: |
          DISTRIBUTION_ID="${{ steps.stack-outputs.outputs.cloudfront-distribution-id }}"
          if [ -n "$DISTRIBUTION_ID" ] && [ "$DISTRIBUTION_ID" != "None" ]; then
            aws cloudfront create-invalidation \
              --distribution-id "$DISTRIBUTION_ID" \
              --paths "/*"
          fi

  deployment-summary:
    name: Foundation Web Deployment Summary
    runs-on: ubuntu-latest
    if: always()
    needs: [deploy]

    steps:
      - name: Print deployment summary
        run: |
          echo "=========================================="
          echo "Foundation Web Deployment Summary"
          echo "=========================================="
          echo "Environment: ${{ env.DEPLOY_ENVIRONMENT }}"
          echo "Stack Name: ${{ env.FOUNDATION_WEB_STACK_NAME }}"
          echo "Region: ${{ env.AWS_REGION }}"
          echo ""
          echo "Frontend URL: ${{ needs.deploy.outputs.frontend-url }}"
          echo "=========================================="

          if [ "${{ needs.deploy.result }}" != "success" ]; then
            echo "Deployment failed"
            exit 1
          fi
          echo "Deployment successful"
