$kind: http-request
name: Pause Reminder
description: Pause an existing reminder by updating its status to `paused`.
method: PUT
url: '{{baseUrl}}/reminders/:reminderId'
order: 4000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
  - key: Content-Type
    value: application/json
pathVariables:
  - key: reminderId
    value: '{{reminderId}}'
    description: UUID of the reminder to pause
body:
  type: json
  content: |-
    {
      "status": "paused"
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });

      pm.test("Reminder is paused", function () {
          const reminder = pm.response.json();
          pm.expect(reminder).to.have.property("id", pm.collectionVariables.get("reminderId"));
          pm.expect(reminder).to.have.property("status", "paused");
      });
