$kind: http-request
name: '4 - Missing Signature Rejected'
description: Send webhook payload without Stripe-Signature and expect rejection.
method: POST
url: '{{baseUrl}}/billing/webhook'
order: 4000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "id": "evt_ci_missing_sig_{{timestamp}}",
      "type": "checkout.session.completed",
      "created": 1767225602,
      "data": {
        "object": {
          "id": "cs_ci_missing_sig",
          "metadata": {
            "user_id": "11111111-1111-4111-8111-111111111111"
          }
        }
      }
    }
scripts:
  - type: beforeRequest
    language: text/javascript
    code: |-
      const ts = Date.now().toString();
      pm.collectionVariables.set("timestamp", ts);
      let payload = pm.request.body.raw;
      payload = payload.replace(/{{timestamp}}/g, ts);
      pm.request.body.update(payload);
      pm.request.headers.remove("Stripe-Signature");
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status code is 400, 500, or 503 when Stripe webhook secret is absent", function () {
          pm.expect([400, 500, 503]).to.include(pm.response.code);
      });

      pm.test("Missing signature path or missing env secret is surfaced", function () {
          const body = pm.response.text();
          pm.expect(
              body.includes("Missing Stripe-Signature")
              || body.includes("STRIPE_WEBHOOK_SECRET")
              || body.includes("Service not configured")
          ).to.be.true;
      });
