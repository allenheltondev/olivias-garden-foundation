$kind: http-request
name: 'Step 5 - Complete Claim'
description: |-
  Transition the claim from confirmed → completed.

  **Depends on**: `claimId` captured in Step 2
  **Asserts**: Response status is `completed`.
method: PUT
url: '{{baseUrl}}/claims/{{claimId}}'
order: 5000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "status": "completed"
    }
scripts:
  - type: beforeRequest
    language: text/javascript
    code: |-
      const claimId = pm.collectionVariables.get("claimId");
      if (!claimId) {
          console.error("claimId not set; aborting.");
          pm.execution.setNextRequest(null);
      }
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });

      pm.test("Claim status is completed", function () {
          const claim = pm.response.json();
          pm.expect(claim).to.have.property("status", "completed");
      });
